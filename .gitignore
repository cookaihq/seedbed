dist/

# Local credential — never commit (contains a GitHub PAT)
.github-token

# Local agent registrations and runtime state
.agent-shell/
.claude/
.codex/

# Project configuration is private; example files remain distributable.
.env
.env.*
!.env.example
!.env.*.example
